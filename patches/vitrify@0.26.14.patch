diff --git a/dist/index.js b/dist/index.js
index 5edfa09bde5c6763edc6493d1d7266b28f4f396a..4bf59afc87b7fa1510820c7e27641192da4eeba6 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -81,6 +81,11 @@ export const VIRTUAL_MODULES = [
  * @returns
  */
 const createCodeSplittingGroups = (ssr) => {
+    // The per-package groups produced circular chunk pairs (vue reactivity
+    // imports quasar directives, quasar components import vue) that crashed the
+    // client at runtime ("RefImpl is not a constructor" / "isFunction is not a
+    // function"). Bundle ALL node_modules into a single acyclic vendor chunk,
+    // while keeping the SSR server entry groups intact.
     return [
         ...VIRTUAL_MODULES.map((m) => ({
             name: m,
@@ -111,20 +116,6 @@ const createCodeSplittingGroups = (ssr) => {
             priority: 40,
             includeDependenciesRecursively: false
         },
-        {
-            test: /(?<!@)quasar/,
-            name: 'quasar'
-        },
-        {
-            test: /@quasar/,
-            name: 'at_quasar'
-        },
-        ...Object.entries(moduleChunks).map(([key, value]) => ({
-            name: key,
-            test: new RegExp(value.join('|')),
-            priority: 20,
-            maxSize: ssr === 'client' || ssr === 'ssg' ? 1000000 : Infinity
-        })),
         {
             name: 'vendor',
             test: /node_modules/,
