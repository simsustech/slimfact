---
"@slimfact/app": patch
---

Payments page: render the filter menu's From/To date inputs in the
configured DATE_FORMAT (DD-MM-YYYY) instead of the raw ISO order, matching
the invoice/bill filter menus.
